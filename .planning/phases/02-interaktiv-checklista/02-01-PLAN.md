---
phase: 02-interaktiv-checklista
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - .prettierrc
  - src/layouts/BaseLayout.astro
  - src/styles/global.css
  - src/pages/index.astro
autonomous: true
requirements: [DESIGN-01, DESIGN-03]

must_haves:
  truths:
    - "npm run dev starts Astro dev server without errors"
    - "Tailwind utility classes render correctly in the browser"
    - "Inter font loads and is applied to body text"
    - "Domain colors from design-tokens are available as Tailwind utilities"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro config with Tailwind vite plugin"
      contains: "tailwindcss"
    - path: "src/styles/global.css"
      provides: "Tailwind import + custom theme with domain colors"
      contains: "@import"
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell with viewport meta, font, lang=sv"
      contains: "lang=\"sv\""
    - path: "src/pages/index.astro"
      provides: "Placeholder page confirming build works"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite.plugins array"
      pattern: "tailwindcss"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "import statement"
      pattern: "import.*global.css"
---

<objective>
Scaffold the Astro 6 project with Tailwind CSS 4, set up the base layout, global styles, and custom theme -- creating the foundation that all subsequent plans build on.

Purpose: No Astro project exists yet. This plan creates the entire project scaffold (package.json, config, layout, styles) while preserving the existing src/data/ JSON files from Phase 1.
Output: A running Astro dev server with Tailwind CSS, Inter font, and domain colors ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-interaktiv-checklista/02-RESEARCH.md

@src/data/design-tokens.json

<interfaces>
<!-- Phase 1 data files -- these exist and must NOT be overwritten -->

From src/data/design-tokens.json:
```json
{
  "domains": {
    "horforstaelse": { "label": "Horforstaelse", "label_short": "Hora", "icon": "Ear", "color_hex": "#D97706", "color_name": "amber" },
    "lasforstaelse": { "label": "Lasforstaelse", "label_short": "Lasa", "icon": "BookOpen", "color_hex": "#2563EB", "color_name": "blue" },
    "muntlig-interaktion": { "label": "Muntlig interaktion", "label_short": "Prata", "icon": "MessageCircle", "color_hex": "#16A34A", "color_name": "green" },
    "muntlig-produktion": { "label": "Muntlig produktion", "label_short": "Beratta", "icon": "Mic", "color_hex": "#7C3AED", "color_name": "violet" },
    "skriftlig-fardighet": { "label": "Skriftlig fardighet", "label_short": "Skriva", "icon": "PenLine", "color_hex": "#DC2626", "color_name": "red" }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project and install dependencies</name>
  <files>package.json, astro.config.mjs, .prettierrc, tsconfig.json</files>
  <action>
CRITICAL: Do NOT use `npm create astro@latest` -- it is interactive and may overwrite src/data/. Instead, manually create the project files.

Per D-01 and D-02: The project root is the current directory. src/data/ already exists and must be preserved.

1. Back up src/data/ by reading all 4 files to confirm they exist before any changes.

2. Create `package.json` with:
   - name: "sfi-spar2-kursmal"
   - type: "module"
   - scripts: { "dev": "astro dev", "build": "astro build", "preview": "astro preview" }
   - dependencies: astro, tailwindcss, @tailwindcss/vite, @lucide/astro, @fontsource/inter
   - devDependencies: prettier, prettier-plugin-astro

3. Create `astro.config.mjs` per RESEARCH.md Pattern:
   ```javascript
   import { defineConfig } from "astro/config";
   import tailwindcss from "@tailwindcss/vite";

   export default defineConfig({
     vite: {
       plugins: [tailwindcss()],
     },
   });
   ```
   CRITICAL: Do NOT use @astrojs/tailwind (deprecated for Tailwind v4). Use @tailwindcss/vite as a Vite plugin.

4. Create `.prettierrc` with: { "plugins": ["prettier-plugin-astro"] }

5. Run `npm install` to install all dependencies.

6. Verify src/data/ still contains design-tokens.json, kurs-b.json, kurs-c.json, kurs-d.json after install.
  </action>
  <verify>
    <automated>cd "C:/Users/alewe/Github/projects/FIRST TEACHER/MINJA HRNJIC" && node -e "const p=require('./package.json'); console.log('astro' in p.dependencies && 'tailwindcss' in p.dependencies && '@tailwindcss/vite' in p.dependencies && '@lucide/astro' in p.dependencies ? 'PASS' : 'FAIL')" && ls src/data/design-tokens.json src/data/kurs-b.json src/data/kurs-c.json src/data/kurs-d.json</automated>
  </verify>
  <done>package.json exists with all 5 production deps, astro.config.mjs uses @tailwindcss/vite plugin, npm install succeeds, src/data/ files intact</done>
</task>

<task type="auto">
  <name>Task 2: Create base layout, global CSS with domain theme, and placeholder page</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <action>
1. Create `src/styles/global.css` with:
   ```css
   @import "tailwindcss";
   @import "@fontsource/inter/latin-400.css";
   @import "@fontsource/inter/latin-600.css";
   @import "@fontsource/inter/latin-700.css";

   @theme {
     --font-sans: "Inter", sans-serif;
     --color-domain-hora: #D97706;
     --color-domain-lasa: #2563EB;
     --color-domain-prata: #16A34A;
     --color-domain-beratta: #7C3AED;
     --color-domain-skriva: #DC2626;
   }

   @keyframes fade-out {
     0% { opacity: 1; }
     70% { opacity: 1; }
     100% { opacity: 0; }
   }

   .saved-indicator {
     animation: fade-out 1.5s ease-out forwards;
   }
   ```
   The domain colors come from design-tokens.json color_hex values. The fade-out animation is for the "Sparat!" indicator (D-12, used in Plan 02).

2. Create `src/layouts/BaseLayout.astro`:
   - Import global.css in the frontmatter
   - HTML lang="sv" (Swedish content)
   - meta viewport: width=device-width, initial-scale=1 (NO maximum-scale per Pitfall 4)
   - meta charset utf-8
   - Title: "SFI Spar 2 -- Kursmal"
   - Body classes: font-sans bg-gray-50 text-gray-900 text-base (per D-21: 16px minimum -- Tailwind text-base is 16px)
   - Per D-19: max-width container centered on desktop. Use a wrapper div with `max-w-2xl mx-auto px-4`
   - Accept a `<slot />` for page content

3. Create `src/pages/index.astro` as a minimal placeholder:
   - Import BaseLayout
   - Wrap content in BaseLayout
   - Add a temporary heading "SFI Spar 2 -- Kursmal" with Tailwind classes to verify styling works
   - This page will be replaced by Plan 02 with the full component tree

4. Run `npm run build` to verify the Astro build completes without errors.
5. Start `npm run dev` briefly to confirm the dev server starts (then stop it).
  </action>
  <verify>
    <automated>cd "C:/Users/alewe/Github/projects/FIRST TEACHER/MINJA HRNJIC" && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>Astro builds successfully. BaseLayout renders with Inter font, gray-50 background, sv lang. Global CSS includes Tailwind, Inter font imports, domain color theme variables, and fade-out keyframes. Placeholder index page loads without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | User-controlled storage -- no sensitive data stored |
| Static file serving | No server-side processing -- all content is build-time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | localStorage data | accept | Checklist state is non-sensitive self-assessment data. Tampering has no impact beyond the user's own view. |
| T-02-02 | Information Disclosure | Student progress | accept | Data stays in browser localStorage only. No network transmission. No PII stored. |
| T-02-03 | Denial of Service | localStorage quota | mitigate | Wrap all localStorage calls in try/catch (implemented in Plan 02 JS). Site works without persistence. |
</threat_model>

<verification>
1. `npm run build` completes with exit code 0
2. Built output in dist/ contains index.html with Inter font reference and Tailwind-generated CSS
3. src/data/ files unchanged from Phase 1
</verification>

<success_criteria>
- Astro dev server starts and serves the placeholder page
- Tailwind utility classes produce visible styling
- Inter font is loaded (visible in Network tab or rendered text)
- Domain color CSS custom properties are defined (inspectable in DevTools)
- No @astrojs/tailwind in config (uses @tailwindcss/vite instead)
- Build output is static HTML (no server runtime)
</success_criteria>

<output>
After completion, create `.planning/phases/02-interaktiv-checklista/02-01-SUMMARY.md`
</output>
