---
phase: 04.1-kunskapstr-d-f-r-alla-kurser
plan: 02
subsystem: trad-page
tags: [multi-course, javascript, localStorage, radar, tab-switching]
dependency_graph:
  requires: [04.1-01]
  provides: [multi-course-interactivity, tab-switching, per-course-radar, shared-localStorage]
  affects: [src/pages/trad.astro]
tech_stack:
  added: []
  patterns: [states-object-per-course, switchTreeTab, course-scoped-dom-queries]
key_files:
  created: []
  modified:
    - src/pages/trad.astro
decisions:
  - "states object holds all three courses in memory — no re-reading localStorage on tab switch"
  - "switchTreeTab only toggles visibility and tab styles — state already in memory from page load"
  - "All DOM queries in toggle handler scoped to course panel via getElementById(tree-panel-course)"
  - "Header subtitle uses unicode escapes for Swedish characters to avoid encoding issues"
metrics:
  duration: 3min
  completed: 2026-04-11
  tasks_completed: 2
  files_modified: 1
---

# Phase 04.1 Plan 02: Multi-Course Tree Page — Client-Side JS Summary

**One-liner:** Rewrote trad.astro client-side JavaScript with multi-course localStorage (sfi-checklist-B/C/D), switchTreeTab function, course-scoped DOM queries, and per-course radar chart updates.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite client-side JavaScript for multi-course interactivity | 3d47e9e | src/pages/trad.astro |
| 2 | Visual verification of multi-course tree page (auto-approved) | — | — |

## What Was Built

**Task 1 — Client-side JS rewrite:**
- Replaced single-course `STORAGE_KEY` + `state` with `COURSES` array and `states` object keyed by course
- `loadState(course)` and `saveState(course, st)` use `sfi-checklist-${course}` keys — same keys as the checklist page
- `switchTreeTab(course)` toggles `course-tree-panel` visibility, updates `tree-tab` button styles with course-specific active classes (cyan/violet/rose), and updates `#tree-header-subtitle`
- Page-load init loop reads all three courses' states and applies checked visual state to each panel's leaves, numbers, and branches
- `toggle()` handler reads `leaf.dataset.course` to scope all DOM queries to `#tree-panel-${course}`
- `showGoalText(course, skillId, goalId, isChecked)` targets `[data-goal-panel="${course}-${skillId}"]`
- `updateProgress(course, skillId)` and `updateCrown(course, skillId)` look up skills from `allSkillsClient[course]`
- `updateRadar(course)` targets `radar-polygon-${course}`, `radar-dot-${id}-${course}`, `pct-${id}-${course}`
- Initial render loop calls `updateProgress`, `updateCrown`, and `updateRadar` for all three courses

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. All three courses are fully wired with real data from JSON files. localStorage keys match the checklist page.

## Self-Check: PASSED

- `src/pages/trad.astro` exists and was modified
- Commit `3d47e9e` exists (Task 1)
- `npx astro build` completed successfully (2 pages built in 2.62s)
- `switchTreeTab` present in trad.astro (2 occurrences)
- `sfi-checklist-` present in trad.astro (2 occurrences)
- `STORAGE_KEY` absent from trad.astro (0 occurrences)
- `activeCourse` present in trad.astro (2 occurrences)
- `radar-polygon-` present in trad.astro (2 occurrences)
