---
phase: 01-innehallsgrund
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/design-tokens.json
  - src/data/kurs-b.json
autonomous: true
requirements:
  - KURS-01
  - KURS-02
  - KURS-03
  - KURS-04
  - KURS-05
  - KURS-06
  - DESIGN-05

must_haves:
  truths:
    - "design-tokens.json contains all 5 skill domains with icon, color, and label"
    - "kurs-b.json contains 'Jag kan...' goals for all 5 skill domains"
    - "Every goal has a stable ID in format B-XXXX-NN"
    - "Every goal has kursplan_year field"
    - "Theme values use only the 7 canonical arshjul strings"
    - "Goal text is written in simple Swedish appropriate for kurs B (CEFR A2)"
  artifacts:
    - path: "src/data/design-tokens.json"
      provides: "Icon, color, and label mappings for 5 skill domains"
      contains: "horforstaelse"
    - path: "src/data/kurs-b.json"
      provides: "Course B simplified goals across all 5 domains"
      contains: "Jag kan"
  key_links:
    - from: "src/data/kurs-b.json"
      to: "src/data/design-tokens.json"
      via: "Matching domain keys"
      pattern: "horforstaelse.*lasforstaelse.*muntlig-interaktion.*muntlig-produktion.*skriftlig-fardighet"
---

<objective>
Create the design token file and the complete kurs B JSON data file -- the foundational pattern that kurs C and D will follow.

Purpose: Establish the canonical schema, domain keys, ID format, and language level baseline. Kurs B has existing source material and is the simplest course, making it the right starting point.
Output: `src/data/design-tokens.json` and `src/data/kurs-b.json` -- ready for Phase 2 consumption.
</objective>

<execution_context>
@C:/Users/alewe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alewe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-innehallsgrund/01-CONTEXT.md
@.planning/phases/01-innehallsgrund/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design-tokens.json and kurs-b.json skeleton</name>
  <files>src/data/design-tokens.json, src/data/kurs-b.json</files>
  <read_first>
    .planning/phases/01-innehallsgrund/01-CONTEXT.md
    .planning/phases/01-innehallsgrund/01-RESEARCH.md
  </read_first>
  <action>
Create the directory `src/data/` if it does not exist.

Create `src/data/design-tokens.json` with this exact content (per D-12, D-13, D-14):

```json
{
  "domains": {
    "horforstaelse": {
      "label": "Horforstaelse",
      "label_short": "Hora",
      "icon": "Ear",
      "color_hex": "#D97706",
      "color_name": "amber"
    },
    "lasforstaelse": {
      "label": "Lasforstaelse",
      "label_short": "Lasa",
      "icon": "BookOpen",
      "color_hex": "#2563EB",
      "color_name": "blue"
    },
    "muntlig-interaktion": {
      "label": "Muntlig interaktion",
      "label_short": "Prata",
      "icon": "MessageCircle",
      "color_hex": "#16A34A",
      "color_name": "green"
    },
    "muntlig-produktion": {
      "label": "Muntlig produktion",
      "label_short": "Beratta",
      "icon": "Mic",
      "color_hex": "#7C3AED",
      "color_name": "violet"
    },
    "skriftlig-fardighet": {
      "label": "Skriftlig fardighet",
      "label_short": "Skriva",
      "icon": "PenLine",
      "color_hex": "#DC2626",
      "color_name": "red"
    }
  }
}
```

NOTE on Swedish characters in labels: Use the actual Swedish diacritics in the label fields (Hor**for**staelse -> **Horforstaelse**, Lasa -> **Lasa**, Beratta -> **Beratta**). Wait -- per RESEARCH.md the labels use diacritics: "Horforstaelse" label is actually "Horforstaelse" with Swedish characters. Use these EXACT label values from RESEARCH.md:
- `"label": "Hörförståelse"`, `"label_short": "Höra"`
- `"label": "Läsförståelse"`, `"label_short": "Läsa"`
- `"label": "Muntlig interaktion"`, `"label_short": "Prata"`
- `"label": "Muntlig produktion"`, `"label_short": "Berätta"`
- `"label": "Skriftlig färdighet"`, `"label_short": "Skriva"`

Domain KEYS must NOT have diacritics (use `horforstaelse`, not `hörförståelse`). Labels DO have diacritics.

Create `src/data/kurs-b.json` with the complete skeleton per D-04:

```json
{
  "course": "B",
  "kursplan_year": 2018,
  "domains": {
    "horforstaelse": {
      "goals": []
    },
    "lasforstaelse": {
      "goals": []
    },
    "muntlig-interaktion": {
      "goals": []
    },
    "muntlig-produktion": {
      "goals": []
    },
    "skriftlig-fardighet": {
      "goals": []
    }
  }
}
```

Each goals array is empty -- Task 2 fills them in. The skeleton ensures the structure is correct before content authoring begins (avoids Pitfall 1 from RESEARCH.md).
  </action>
  <verify>
    <automated>node -e "const dt=require('./src/data/design-tokens.json'); const kb=require('./src/data/kurs-b.json'); const dk=Object.keys(dt.domains); const bk=Object.keys(kb.domains); console.log('design-tokens domains:', dk.length); console.log('kurs-b domains:', bk.length); console.log('keys match:', JSON.stringify(dk)===JSON.stringify(bk)); if(dk.length!==5||bk.length!==5||JSON.stringify(dk)!==JSON.stringify(bk)) process.exit(1); console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/design-tokens.json exists and is valid JSON
    - design-tokens.json contains exactly 5 domain keys: horforstaelse, lasforstaelse, muntlig-interaktion, muntlig-produktion, skriftlig-fardighet
    - Each domain in design-tokens.json has fields: label, label_short, icon, color_hex, color_name
    - design-tokens.json icon values are exactly: Ear, BookOpen, MessageCircle, Mic, PenLine
    - src/data/kurs-b.json exists and is valid JSON
    - kurs-b.json has "course": "B" and "kursplan_year": 2018
    - kurs-b.json domain keys exactly match design-tokens.json domain keys (no underscore vs hyphen mismatch)
    - No diacritics (a, o, a) appear in any domain KEY (only in label values)
  </acceptance_criteria>
  <done>Both JSON files exist with correct structure, matching domain keys, and valid JSON syntax.</done>
</task>

<task type="auto">
  <name>Task 2: Author kurs B goals with themes</name>
  <files>src/data/kurs-b.json</files>
  <read_first>
    src/data/kurs-b.json
    src/data/design-tokens.json
    Kursmål och checklista för elever.md
    Årshjul för sfi 2 kurs B, C och D .md
    .planning/phases/01-innehallsgrund/01-RESEARCH.md
  </read_first>
  <action>
Fill in the goals arrays in `src/data/kurs-b.json`. This is the content authoring task -- the highest-risk work in Phase 1.

**Source material:** Use `Kursmål och checklista för elever.md` as the PRIMARY source for kurs B. That file contains Skolverket criteria text for Läsa, Muntligt, Skriftligt, and Höra. Transform the official criteria into "Jag kan..." statements.

**ID format (per D-02):** `B-{DOMAIN}-{NN}` where DOMAIN is one of: HORA, LASA, MINT, MPRO, SKRI. Pad NN to two digits.

**Authoring rules (per D-05, D-06, DESIGN-05, RESEARCH.md):**
1. Every goal text starts with "Jag kan" (NOT "Du kan", NOT "Eleven kan")
2. Each statement must be concrete and yes/no checkable
3. One competency per statement (no compound "och" goals)
4. Kurs B = CEFR A2 vocabulary: simple words, no abstractions, max one clause per sentence
5. No Skolverket jargon: use "samtal" not "interaktion", "berätta" not "produktion"
6. No "Jag ska..." items (those are Phase 3 scope -- Pitfall 2 from RESEARCH.md)

**Target: 4-8 goals per domain, ~5 is ideal** (per RESEARCH.md A5).

**Goals to create based on the source material:**

horforstaelse (from "Höra" section of source file):
- B-HORA-01: "Jag kan förstå enkelt tal i vanliga situationer." themes: []
- B-HORA-02: "Jag kan förstå korta berättelser om välbekanta ämnen." themes: []
- B-HORA-03: "Jag kan förstå korta meddelanden, till exempel på en tågstation." themes: ["fritid-och-halsa"]
- B-HORA-04: "Jag kan förstå enkla instruktioner i klassrummet." themes: ["arbete-och-studier"]
- B-HORA-05: "Jag kan förstå när någon berättar om sin familj eller sitt arbete." themes: ["relationer", "arbete-och-studier"]

lasforstaelse (from "Läsa" section -- 5 bullet points in source):
- B-LASA-01: "Jag kan läsa och förstå enkla texter om vardagliga ämnen." themes: []
- B-LASA-02: "Jag kan förstå enkel information, till exempel 'ställ upp stolarna'." themes: ["arbete-och-studier"]
- B-LASA-03: "Jag kan förstå korta instruktioner, till exempel 'kryssa för rätt svar'." themes: ["arbete-och-studier"]
- B-LASA-04: "Jag kan förstå korta meddelanden, till exempel att skolan är stängd." themes: ["arbete-och-studier"]
- B-LASA-05: "Jag kan läsa och förstå mitt schema." themes: ["arbete-och-studier"]

muntlig-interaktion (from "Muntligt" first paragraph -- conversation/interaction):
- B-MINT-01: "Jag kan svara på enkla frågor i ett samtal." themes: []
- B-MINT-02: "Jag kan ställa enkla frågor i ett samtal." themes: []
- B-MINT-03: "Jag kan berätta vad jag tycker med enkla ord." themes: []
- B-MINT-04: "Jag kan prata om välbekanta ämnen, till exempel familj och arbete." themes: ["relationer", "arbete-och-studier"]
- B-MINT-05: "Jag kan hålla ett kort samtal med en annan person." themes: []

muntlig-produktion (from "Muntligt" second paragraph -- one-way speaking):
- B-MPRO-01: "Jag kan berätta om mig själv med enkla meningar." themes: []
- B-MPRO-02: "Jag kan berätta om min dag för klassen." themes: []
- B-MPRO-03: "Jag kan beskriva en person eller en plats med enkla ord." themes: []
- B-MPRO-04: "Jag kan berätta om något som har hänt." themes: []

skriftlig-fardighet (from "Skriftligt" section):
- B-SKRI-01: "Jag kan skriva korta och enkla meningar." themes: []
- B-SKRI-02: "Jag kan skriva ett kort meddelande, till exempel ett sms." themes: []
- B-SKRI-03: "Jag kan skriva om mig själv, till exempel mitt namn, min adress och mitt telefonnummer." themes: []
- B-SKRI-04: "Jag kan skriva en kort hälsning." themes: ["relationer", "hogtider"]
- B-SKRI-05: "Jag kan skriva enkla texter om saker jag har gjort." themes: []

All goals get `"kursplan_year": 2018`.

**Theme assignment rules (per D-09, D-10, D-11):**
- Only use these 7 exact strings: `relationer`, `miljo`, `bostad`, `arbete-och-studier`, `fritid-och-halsa`, `hogtider`, `samhalle`
- Assign conservatively -- only when the goal genuinely connects to a theme
- Generic language skills get `"themes": []`
- NO underscores, NO diacritics in theme strings (Pitfall 4 from RESEARCH.md)
  </action>
  <verify>
    <automated>node -e "const kb=require('./src/data/kurs-b.json'); const validThemes=['relationer','miljo','bostad','arbete-och-studier','fritid-och-halsa','hogtider','samhalle']; let errs=[]; let total=0; for(const[dk,dv] of Object.entries(kb.domains)){const gs=dv.goals; if(gs.length<4||gs.length>8) errs.push(dk+' has '+gs.length+' goals (want 4-8)'); total+=gs.length; gs.forEach((g,i)=>{if(!g.id.startsWith('B-')) errs.push(g.id+' bad prefix'); if(!g.text.startsWith('Jag kan')) errs.push(g.id+' text must start with Jag kan'); if(typeof g.kursplan_year!=='number') errs.push(g.id+' missing kursplan_year'); if(!Array.isArray(g.themes)) errs.push(g.id+' themes not array'); g.themes.forEach(t=>{if(!validThemes.includes(t)) errs.push(g.id+' invalid theme: '+t)});})} if(errs.length){console.error(errs.join('\n'));process.exit(1)} console.log('Total goals:',total,'across 5 domains. PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - kurs-b.json contains goals in all 5 domains
    - Each domain has 4-8 goals
    - Total goal count is between 20 and 40
    - Every goal ID matches pattern B-(HORA|LASA|MINT|MPRO|SKRI)-\d{2}
    - Every goal text starts with "Jag kan"
    - No goal text contains "ska" (those are Phase 3 scope)
    - Every goal has kursplan_year: 2018
    - Every goal has a themes array (may be empty)
    - All theme values are from the set: relationer, miljo, bostad, arbete-och-studier, fritid-och-halsa, hogtider, samhalle
    - No theme string contains underscores or Swedish diacritics
    - IDs are sequential within each domain (01, 02, 03...) with no gaps
  </acceptance_criteria>
  <done>kurs-b.json contains 20-40 "Jag kan..." goals across all 5 domains, with stable IDs, kursplan_year, and theme tags. Validation script passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher -> JSON files | Teacher edits JSON directly; malformed JSON breaks Astro build |
| JSON files -> Astro build (Phase 2) | Build-time import; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | kurs-b.json | accept | Static files served from build output; teacher is trusted editor. Astro build fails on malformed JSON, providing implicit validation. |
| T-01-02 | Information Disclosure | design-tokens.json | accept | No sensitive data in design tokens or course goals. All content is public educational material. |
</threat_model>

<verification>
1. Run the Task 1 verification script: confirms both files exist, are valid JSON, have 5 matching domain keys
2. Run the Task 2 verification script: confirms all goals have correct IDs, "Jag kan" prefix, valid themes, kursplan_year
3. Manual spot-check: open kurs-b.json and verify goal text reads as simple, concrete Swedish at kurs B level
</verification>

<success_criteria>
- design-tokens.json contains 5 domains with icon, color_hex, label, label_short
- kurs-b.json contains 20-40 goals across 5 domains
- Domain keys match exactly between both files
- All goal IDs follow B-XXXX-NN format
- All themes use canonical arshjul strings
- Both files are valid JSON importable by Astro
</success_criteria>

<output>
After completion, create `.planning/phases/01-innehallsgrund/01-01-SUMMARY.md`
</output>
