---
phase: 01-innehallsgrund
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/data/kurs-c.json
  - src/data/kurs-d.json
autonomous: true
requirements:
  - KURS-01
  - KURS-02
  - KURS-04
  - KURS-05
  - KURS-06
  - DESIGN-05

must_haves:
  truths:
    - "kurs-c.json contains 'Jag kan...' goals for all 5 skill domains at CEFR B1 level"
    - "kurs-d.json contains 'Jag kan...' goals for all 5 skill domains at CEFR B2 level"
    - "Every goal has a stable ID in format C-XXXX-NN or D-XXXX-NN"
    - "Every goal has kursplan_year field"
    - "Theme values use only the 7 canonical arshjul strings"
    - "Domain keys in kurs-c.json and kurs-d.json match kurs-b.json exactly"
    - "Goal complexity increases from B to C to D while structure stays identical"
  artifacts:
    - path: "src/data/kurs-c.json"
      provides: "Course C simplified goals across all 5 domains"
      contains: "Jag kan"
    - path: "src/data/kurs-d.json"
      provides: "Course D simplified goals across all 5 domains"
      contains: "Jag kan"
  key_links:
    - from: "src/data/kurs-c.json"
      to: "src/data/design-tokens.json"
      via: "Matching domain keys"
      pattern: "horforstaelse.*lasforstaelse.*muntlig-interaktion.*muntlig-produktion.*skriftlig-fardighet"
    - from: "src/data/kurs-d.json"
      to: "src/data/design-tokens.json"
      via: "Matching domain keys"
      pattern: "horforstaelse.*lasforstaelse.*muntlig-interaktion.*muntlig-produktion.*skriftlig-fardighet"
---

<objective>
Author the complete kurs C and kurs D JSON data files, following the pattern established in kurs B (Plan 01).

Purpose: Complete the course content for all three SFI spar 2 courses. Kurs C and D have no existing simplified material -- they must be authored from scratch based on Skolverket's official betygskriterier, using kurs B as the structural and linguistic template.
Output: `src/data/kurs-c.json` and `src/data/kurs-d.json` -- completing the full content foundation for Phase 2.
</objective>

<execution_context>
@C:/Users/alewe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alewe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-innehallsgrund/01-CONTEXT.md
@.planning/phases/01-innehallsgrund/01-RESEARCH.md
@.planning/phases/01-innehallsgrund/01-01-SUMMARY.md

<interfaces>
<!-- Domain keys and JSON structure from Plan 01 outputs -->

From src/data/kurs-b.json (reference structure):
```json
{
  "course": "B",
  "kursplan_year": 2018,
  "domains": {
    "horforstaelse": { "goals": [{ "id": "B-HORA-01", "text": "Jag kan...", "kursplan_year": 2018, "themes": [] }] },
    "lasforstaelse": { "goals": [...] },
    "muntlig-interaktion": { "goals": [...] },
    "muntlig-produktion": { "goals": [...] },
    "skriftlig-fardighet": { "goals": [...] }
  }
}
```

Domain keys (MUST match exactly): horforstaelse, lasforstaelse, muntlig-interaktion, muntlig-produktion, skriftlig-fardighet
ID domain codes: HORA, LASA, MINT, MPRO, SKRI
Valid themes: relationer, miljo, bostad, arbete-och-studier, fritid-och-halsa, hogtider, samhalle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author kurs C goals</name>
  <files>src/data/kurs-c.json</files>
  <read_first>
    src/data/kurs-b.json
    src/data/design-tokens.json
    Kursmål och checklista för elever.md
    Årshjul för sfi 2 kurs B, C och D .md
    .planning/phases/01-innehallsgrund/01-RESEARCH.md
  </read_first>
  <action>
Create `src/data/kurs-c.json` with the same structure as kurs-b.json (per D-07 -- same structural format, complexity increases in text content only).

**Top-level:** `"course": "C"`, `"kursplan_year": 2018`

**ID format:** `C-{DOMAIN}-{NN}` (e.g., C-HORA-01, C-LASA-01)

**Language level (per D-06, DESIGN-05):** Kurs C = CEFR B1. Compared to kurs B:
- More subordinate clauses are acceptable
- Slightly more abstract vocabulary allowed
- Longer sentences permitted (but still clear)
- Topics expand beyond immediate personal life to society, media, work situations

**Kurs C is the intermediate course.** Goals should reflect that the student can:
- Handle more complex listening/reading situations (not just "enkla")
- Participate in conversations about broader topics
- Express opinions with some reasoning
- Write connected texts (not just short messages)

**Goal authoring -- follow these guidelines for each domain:**

horforstaelse (C-HORA-01 through C-HORA-05/06):
Goals about understanding spoken Swedish in varied situations -- news, conversations about society, instructions at work. Kurs C students understand more than basic everyday talk.
Example goals:
- "Jag kan forsta huvudinnehallet i korta nyhetsinslag."
- "Jag kan forsta ett samtal om ett aktuellt amne."
- "Jag kan forsta instruktioner pa en arbetsplats."
- "Jag kan forsta nar nagon forklarar hur man gor nagot."
- "Jag kan forsta olika asikter i en diskussion om ett vanligt amne."
(Use Swedish diacritics in actual file: forsta -> förstå, etc.)

lasforstaelse (C-LASA-01 through C-LASA-05/06):
Goals about reading newspaper articles, simple factual texts, instructions.
Example goals:
- "Jag kan lasa och forsta enkla tidningsartiklar om aktuella amnen."
- "Jag kan forsta skriftliga instruktioner for en uppgift pa jobbet."
- "Jag kan lasa och forsta information fran myndigheter, till exempel Forsakringskassan."
- "Jag kan forsta huvudinnehallet i en enkel berattande text."
- "Jag kan hitta specifik information i en langre text."

muntlig-interaktion (C-MINT-01 through C-MINT-05):
Goals about participating in discussions, expressing opinions, handling everyday service situations.
Example goals:
- "Jag kan delta i ett samtal om ett aktuellt amne och saga vad jag tycker."
- "Jag kan forklara och motivera mina asikter i en diskussion."
- "Jag kan fraga om och beskriva saker i en afar eller pa ett kontor."
- "Jag kan diskutera planer och forslag med andra."
- "Jag kan hantera ovaantade situationer i ett samtal, till exempel missforstand."

muntlig-produktion (C-MPRO-01 through C-MPRO-04/05):
Goals about presenting, describing in detail, narrating events with some structure.
Example goals:
- "Jag kan beratta om en upplevelse sa att andra forstar vad som hande."
- "Jag kan presentera ett amne som jag har forberett."
- "Jag kan beskriva mina planer och onskemal for framtiden."
- "Jag kan ge en kort sammanfattning av en text eller film."

skriftlig-fardighet (C-SKRI-01 through C-SKRI-05):
Goals about writing connected texts, emails, simple descriptions.
Example goals:
- "Jag kan skriva en enkel sammanhangende text om ett bekant amne."
- "Jag kan skriva ett mejl med en fraga eller en forklaring."
- "Jag kan beskriva en person, en plats eller en handelse i skrift."
- "Jag kan skriva en kort text dar jag uttrycker min asikt."
- "Jag kan anvanda skiljetecken och styckeindelning i mina texter."

IMPORTANT: The examples above are GUIDELINES. Use proper Swedish diacritics (a -> a, o -> o, a -> a as appropriate: forsta -> förstå, amne -> ämne, asikt -> åsikt, etc.). Adjust wording to be natural, concrete, and checkable.

**Theme assignment:** Apply themes more broadly than kurs B since C covers wider topics. Use the same 7 canonical theme strings. Tag conservatively -- only genuine connections.

**Target:** 4-8 goals per domain (aim for 5).
  </action>
  <verify>
    <automated>node -e "const kc=require('./src/data/kurs-c.json'); const kb=require('./src/data/kurs-b.json'); const validThemes=['relationer','miljo','bostad','arbete-och-studier','fritid-och-halsa','hogtider','samhalle']; let errs=[]; if(kc.course!=='C') errs.push('course must be C'); if(JSON.stringify(Object.keys(kc.domains))!==JSON.stringify(Object.keys(kb.domains))) errs.push('domain keys dont match kurs-b'); let total=0; for(const[dk,dv] of Object.entries(kc.domains)){const gs=dv.goals; if(gs.length<4||gs.length>8) errs.push(dk+' has '+gs.length+' goals'); total+=gs.length; gs.forEach(g=>{if(!g.id.startsWith('C-')) errs.push(g.id+' bad prefix'); if(!g.text.startsWith('Jag kan')) errs.push(g.id+' text must start with Jag kan'); if(typeof g.kursplan_year!=='number') errs.push(g.id+' missing kursplan_year'); if(!Array.isArray(g.themes)) errs.push(g.id+' themes not array'); g.themes.forEach(t=>{if(!validThemes.includes(t)) errs.push(g.id+' invalid theme: '+t)});})} if(errs.length){console.error(errs.join('\n'));process.exit(1)} console.log('kurs-c.json: Total goals:',total,'PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/kurs-c.json exists and is valid JSON
    - kurs-c.json has "course": "C" and "kursplan_year": 2018
    - Domain keys match kurs-b.json exactly (same order, same spelling)
    - Each domain has 4-8 goals
    - Every goal ID matches pattern C-(HORA|LASA|MINT|MPRO|SKRI)-\d{2}
    - Every goal text starts with "Jag kan"
    - No goal text contains "ska"
    - Every goal has kursplan_year: 2018
    - All theme values are from the canonical 7-string set
    - Goal text uses CEFR B1-level vocabulary (more complex than kurs B)
    - IDs are sequential within each domain with no gaps
  </acceptance_criteria>
  <done>kurs-c.json contains 20-40 "Jag kan..." goals at CEFR B1 level, with stable IDs, valid themes, and structure matching kurs-b.json exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Author kurs D goals</name>
  <files>src/data/kurs-d.json</files>
  <read_first>
    src/data/kurs-b.json
    src/data/kurs-c.json
    src/data/design-tokens.json
    Årshjul för sfi 2 kurs B, C och D .md
    .planning/phases/01-innehallsgrund/01-RESEARCH.md
  </read_first>
  <action>
Create `src/data/kurs-d.json` with the same structure as kurs-b.json and kurs-c.json.

**Top-level:** `"course": "D"`, `"kursplan_year": 2018`

**ID format:** `D-{DOMAIN}-{NN}` (e.g., D-HORA-01, D-LASA-01)

**Language level (per D-06, DESIGN-05):** Kurs D = CEFR B2. The most advanced SFI course:
- More nuanced distinctions between competencies
- Abstract and societal vocabulary expected
- Complex sentence structures permitted
- Topics include societal issues, workplace communication, formal situations

**Kurs D students can:**
- Understand main content of complex speech and text
- Participate actively in discussions about abstract topics
- Write well-structured texts with clear argumentation
- Handle formal and informal register

**Goal authoring per domain:**

horforstaelse (D-HORA-01 through D-HORA-05/06):
Understanding complex speech, debates, presentations, varied accents.
Example goals:
- "Jag kan forsta huvudinnehallet i ett langre samtal om ett samhallsamne."
- "Jag kan forsta olika talare i en diskussion, aven nar de har olika asikter."
- "Jag kan forsta nyheter och dokumentarer om aktuella amnen."
- "Jag kan forsta forelasningar eller presentationer om bekanta amnen."
- "Jag kan forsta nar nagon argumenterar for eller emot nagot."
(Use proper Swedish diacritics in actual file.)

lasforstaelse (D-LASA-01 through D-LASA-05/06):
Reading argumentative texts, formal letters, longer articles.
Example goals:
- "Jag kan lasa och forsta langre tidningsartiklar om samhallsfragor."
- "Jag kan forsta formella brev fran myndigheter."
- "Jag kan lasa en argumenterande text och forsta bade for- och motargument."
- "Jag kan forsta huvudinnehallet i enkla skonfitterara texter."
- "Jag kan jamfora information fran olika texter om samma amne."

muntlig-interaktion (D-MINT-01 through D-MINT-05):
Active debate participation, handling disagreement, formal conversations.
Example goals:
- "Jag kan delta aktivt i en diskussion om ett samhallsamne."
- "Jag kan argumentera for min asikt och bemota andras argument."
- "Jag kan anpassa mitt sprak till situationen, till exempel formellt eller informellt."
- "Jag kan diskutera for- och nackdelar med olika losningar."
- "Jag kan forhandla och kompromissa i ett samtal."

muntlig-produktion (D-MPRO-01 through D-MPRO-04/05):
Structured presentations, detailed descriptions, argumentation.
Example goals:
- "Jag kan halla en kort presentation om ett amne jag har forberett."
- "Jag kan beratta om en handelse pa ett strukturerat satt med inledning, mitt och slut."
- "Jag kan motivera mina asikter med exempel och forklaringar."
- "Jag kan sammanfatta ett langre samtal eller en text muntligt."

skriftlig-fardighet (D-SKRI-01 through D-SKRI-05/06):
Structured texts, formal writing, argumentation in writing.
Example goals:
- "Jag kan skriva en sammanhangande text med tydlig struktur."
- "Jag kan skriva ett formellt mejl, till exempel till en myndighet."
- "Jag kan uttrycka och motivera min asikt i skrift."
- "Jag kan skriva en text dar jag jamfor tva saker eller ideer."
- "Jag kan anvanda varierade meningar och bindord i mina texter."

IMPORTANT: Use proper Swedish diacritics in all goal text. The examples above show ASCII -- the actual file must have ö, å, ä etc. Ensure goals are distinctly more advanced than kurs C while remaining checkable.

**Theme assignment:** Kurs D covers more societal topics. Use `samhalle` theme more frequently. Use `arbete-och-studier` for workplace goals. Keep generic language-skill goals with empty themes.

**Target:** 4-8 goals per domain (aim for 5).
  </action>
  <verify>
    <automated>node -e "const kd=require('./src/data/kurs-d.json'); const kb=require('./src/data/kurs-b.json'); const validThemes=['relationer','miljo','bostad','arbete-och-studier','fritid-och-halsa','hogtider','samhalle']; let errs=[]; if(kd.course!=='D') errs.push('course must be D'); if(JSON.stringify(Object.keys(kd.domains))!==JSON.stringify(Object.keys(kb.domains))) errs.push('domain keys dont match kurs-b'); let total=0; for(const[dk,dv] of Object.entries(kd.domains)){const gs=dv.goals; if(gs.length<4||gs.length>8) errs.push(dk+' has '+gs.length+' goals'); total+=gs.length; gs.forEach(g=>{if(!g.id.startsWith('D-')) errs.push(g.id+' bad prefix'); if(!g.text.startsWith('Jag kan')) errs.push(g.id+' text must start with Jag kan'); if(typeof g.kursplan_year!=='number') errs.push(g.id+' missing kursplan_year'); if(!Array.isArray(g.themes)) errs.push(g.id+' themes not array'); g.themes.forEach(t=>{if(!validThemes.includes(t)) errs.push(g.id+' invalid theme: '+t)});})} if(errs.length){console.error(errs.join('\n'));process.exit(1)} console.log('kurs-d.json: Total goals:',total,'PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/kurs-d.json exists and is valid JSON
    - kurs-d.json has "course": "D" and "kursplan_year": 2018
    - Domain keys match kurs-b.json exactly
    - Each domain has 4-8 goals
    - Every goal ID matches pattern D-(HORA|LASA|MINT|MPRO|SKRI)-\d{2}
    - Every goal text starts with "Jag kan"
    - No goal text contains "ska"
    - Every goal has kursplan_year: 2018
    - All theme values are from the canonical 7-string set
    - Goal text uses CEFR B2-level vocabulary (more complex than kurs C)
    - IDs are sequential within each domain with no gaps
  </acceptance_criteria>
  <done>kurs-d.json contains 20-40 "Jag kan..." goals at CEFR B2 level, with stable IDs, valid themes, and structure identical to kurs-b.json and kurs-c.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher -> JSON files | Teacher edits JSON directly; malformed JSON breaks Astro build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | kurs-c.json, kurs-d.json | accept | Same as Plan 01: teacher is trusted editor, Astro build validates JSON syntax. |
| T-01-02 | Information Disclosure | course goal text | accept | All content is public Skolverket curriculum material. No PII or secrets. |
</threat_model>

<verification>
1. Run Task 1 verification script: confirms kurs-c.json structure, IDs, themes
2. Run Task 2 verification script: confirms kurs-d.json structure, IDs, themes
3. Cross-file domain key consistency check:
```bash
node -e "const b=require('./src/data/kurs-b.json');const c=require('./src/data/kurs-c.json');const d=require('./src/data/kurs-d.json');const bk=JSON.stringify(Object.keys(b.domains));const ck=JSON.stringify(Object.keys(c.domains));const dk=JSON.stringify(Object.keys(d.domains));if(bk!==ck||bk!==dk){console.error('DOMAIN KEY MISMATCH');process.exit(1)}console.log('All 3 course files have matching domain keys. PASS');"
```
4. Total goal count across all files should be 60-120 (3 courses x 5 domains x 4-8 goals)
</verification>

<success_criteria>
- kurs-c.json and kurs-d.json exist with correct structure matching kurs-b.json
- Domain keys match across all 3 course files AND design-tokens.json
- All IDs follow the C-XXXX-NN / D-XXXX-NN pattern
- Language complexity increases visibly from B to C to D
- All themes use canonical strings
- Combined total of 60-120 goals across all 3 courses
</success_criteria>

<output>
After completion, create `.planning/phases/01-innehallsgrund/01-02-SUMMARY.md`
</output>
